# PostgreSQL Client Authentication Configuration
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections
local   all             all                                     trust

# IPv4 local connections
host    all             all             127.0.0.1/32            trust

# WireGuard overlay network - application access
host    all             all             10.88.0.0/24            md5

# Replication connections from replicas
host    replication     replicator      10.88.0.12/32           md5
host    replication     replicator      10.88.0.13/32           md5

# Reject all other connections
host    all             all             0.0.0.0/0               reject
